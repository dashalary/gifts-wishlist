get '/items/:id/edit' do
    if !logged_in?
        redirect '/login'
    else
        if item = current_user.items.find(params[:id])
            erb :'items/edit_item'
        else
        redirect '/items'
    end
    end
end